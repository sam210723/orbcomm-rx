{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Orbcomm Decoder (UDP)",
            "type": "shell",
            "command": "python decoder\\orbcomm-decoder.py --dump decoder\\frames.bin"
        },
        {
            "label": "Orbcomm Decoder (File)",
            "type": "shell",
            "command": "python decoder\\orbcomm-decoder.py --file demodulator\\sample-symbols.bin --dump decoder\\frames.bin"
        }
    ]
}
